{
  "name": "@vex-os/audit",
  "version": "0.1.0",
  "private": true,
  "description": "Append-only, chain-hashed audit logging service (SAD §4.6 / SEC-001 §6)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@vex-os/database": "*",
    "@vex-os/shared": "*",
    "drizzle-orm": "^0.38.2"
  }
}
