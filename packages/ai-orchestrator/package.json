{
  "name": "@vex-os/ai-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-agnostic InferenceProvider adapter layer — the only place LLM provider SDKs may be imported",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@vex-os/shared": "*",
    "@anthropic-ai/sdk": "^0.32.1",
    "handlebars": "^4.7.8",
    "zod": "^3.24.1"
  }
}
